*.bak
.DS_Store
# A stray lefthook.yml (e.g. a contributor ran `lefthook install` by hand) — omakase no
# longer uses lefthook; ignore it so a leftover never gets committed here.
/lefthook.yml

# Local scratch — never ship to origin
docs/notes/
docs/specs/
docs/superpowers/
dist/
